meta {
  name: cleanup product
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/products/{{tcProductId}}
  body: none
  auth: none
}

script:pre-request {
  if (!bru.getVar("tcProductId")) bru.runner.skipRequest();
}

headers {
  Token: {{token}}
}

script:post-response {
  bru.setVar("tcProductId", "");
}

tests {
  test("200", function() { expect(res.getStatus()).to.equal(200); });
}
