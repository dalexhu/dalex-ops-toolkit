# copy to .env (git-ignored) and fill in; Bruno reads it from the collection root.
# 默认跑自动化用 e2e_test（禅道 83 上的专用账号，管理员权限组，不是公司管理员）。
# 值用单引号包起来：密码里若含 $ 、反引号或空格，shell 的 `source ./.env` 会改写它
# （bru 自己解析 .env，不受影响；但 README 里的 curl 示例会 source 这个文件）。
ZENTAO_PASSWORD='e2e_test 的密码'
