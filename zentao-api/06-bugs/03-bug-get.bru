meta {
  name: bug get
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/bugs/{{bugId}}
  body: none
  auth: none
}

script:pre-request {
  if (!bru.getVar("bugId")) bru.runner.skipRequest();
}

headers {
  Token: {{token}}
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
