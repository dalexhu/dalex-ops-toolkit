meta {
  name: project detail
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/projects/{{projectId}}
  body: none
  auth: none
}

headers {
  Token: {{token}}
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
