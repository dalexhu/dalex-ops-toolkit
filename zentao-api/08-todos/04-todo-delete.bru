meta {
  name: todo delete
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/todos/{{todoId}}
  body: none
  auth: none
}

script:pre-request {
  if (!bru.getVar("todoId")) bru.runner.skipRequest();
}

headers {
  Token: {{token}}
}

script:post-response {
  bru.setVar("todoId", "");
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
