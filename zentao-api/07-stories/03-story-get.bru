meta {
  name: story get
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/stories/{{storyId}}
  body: none
  auth: none
}

script:pre-request {
  if (!bru.getVar("storyId")) bru.runner.skipRequest();
}

headers {
  Token: {{token}}
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
